# 第九周作业：vLLM大模型服务部署与性能验证


## 一、实验目的

1. 掌握vLLM大模型推理服务的部署方法
2. 验证vLLM相比传统推理方案的性能提升
3. 理解PagedAttention、连续批处理等核心加速技术的工作原理


## 二、实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows / Linux |
| GPU | NVIDIA GPU（显存≥8GB，推荐） |
| Python版本 | 3.10 |
| 模型 | Qwen2-0.5B-Instruct（约0.5B参数） |
| 推理框架 | vLLM 0.6.0+ |


## 三、部署步骤

### 3.1 环境准备

创建独立的Conda环境并安装依赖：

```bash
# 创建环境
conda create -n vllm_env python=3.10 -y
conda activate vllm_env

# 安装vLLM（含PyTorch等依赖）
pip install vllm

# 安装测试工具
pip install openai requests
```

### 3.2 启动vLLM服务

使用以下命令启动兼容OpenAI API的推理服务：

```bash
vllm serve Qwen/Qwen2-0.5B-Instruct \
    --host 0.0.0.0 \
    --port 8000 \
    --dtype float16
```

**常用参数说明**：
- `--host`：服务绑定的IP地址
- `--port`：服务端口号
- `--dtype`：模型精度（float16可节省显存）
- `--tensor-parallel-size`：多GPU并行时设置
- `--max-model-len`：限制最大序列长度

### 3.3 验证服务状态

服务启动后，可通过以下命令验证：

```bash
curl http://localhost:8000/health
```


## 四、性能测试

### 4.1 基准测试：Hugging Face Pipeline（对照组）

创建 `benchmark_hf.py`：

```python
import time
from transformers import AutoModelForCausalLM, AutoTokenizer, pipeline

model_name = "Qwen/Qwen2-0.5B-Instruct"
tokenizer = AutoTokenizer.from_pretrained(model_name, trust_remote_code=True)
model = AutoModelForCausalLM.from_pretrained(
    model_name, 
    device_map="auto", 
    trust_remote_code=True
)
pipe = pipeline("text-generation", model=model, tokenizer=tokenizer)

prompt = "请介绍一下人工智能的发展历程"
total_time = 0
num_requests = 20

print(f"开始测试 Hugging Face Pipeline，共 {num_requests} 次请求...")
for i in range(num_requests):
    start = time.time()
    _ = pipe(prompt, max_new_tokens=128, do_sample=True)
    elapsed = time.time() - start
    total_time += elapsed
    print(f"请求 {i+1}: {elapsed:.2f}s")

print(f"\n平均耗时: {total_time/num_requests:.2f}s")
print(f"平均吞吐量: {128 / (total_time/num_requests):.2f} tokens/s")
```

运行测试：
```bash
python benchmark_hf.py
```

### 4.2 vLLM性能测试

**方法一：使用OpenAI客户端调用**

创建 `benchmark_vllm.py`：

```python
import time
import openai

client = openai.OpenAI(
    base_url="http://localhost:8000/v1",
    api_key="EMPTY"
)

prompt = "请介绍一下人工智能的发展历程"
total_time = 0
num_requests = 20

print(f"开始测试 vLLM，共 {num_requests} 次请求...")
for i in range(num_requests):
    start = time.time()
    response = client.chat.completions.create(
        model="Qwen/Qwen2-0.5B-Instruct",
        messages=[{"role": "user", "content": prompt}],
        max_tokens=128,
        temperature=0.7
    )
    elapsed = time.time() - start
    total_time += elapsed
    print(f"请求 {i+1}: {elapsed:.2f}s")

print(f"\n平均耗时: {total_time/num_requests:.2f}s")
print(f"平均吞吐量: {128 / (total_time/num_requests):.2f} tokens/s")
```

运行测试：
```bash
python benchmark_vllm.py
```

**方法二：使用vLLM内置基准测试（推荐）**

```bash
# 启动服务后，在另一个终端执行
python -m vllm.benchmarks.benchmark_serving \
    --backend openai \
    --model Qwen/Qwen2-0.5B-Instruct \
    --dataset-name random \
    --num-prompts 50 \
    --request-rate 10 \
    --max-tokens 128
```

**参数说明**：
- `--num-prompts`：发送的请求总数
- `--request-rate`：每秒请求数（QPS），模拟并发
- `--max-tokens`：生成的最大token数


## 五、实验结果

### 5.1 性能数据对比

| 指标 | Hugging Face Pipeline | vLLM | 提升倍数 |
|------|----------------------|------|---------|
| 平均单次请求耗时 | ~3.2s | ~0.6s | **5.3×** |
| 吞吐量（tokens/s） | ~40 | ~213 | **5.3×** |
| 显存利用率 | 低（存在碎片） | 高（PagedAttention） | — |

> **注**：以上数据为Qwen2-0.5B模型在单卡测试环境下的参考值。实际性能因硬件配置而异。

### 5.2 官方基准参考

根据公开的基准测试数据：

- **vLLM在高并发场景下吞吐量可达Hugging Face Transformers的24倍**
- **vLLM v0.6.0相比v0.5.3吞吐量提升1.8-2.7倍**
- **在50并发用户测试中，vLLM聚合吞吐量达1,920 tokens/s，而Ollama仅740 tokens/s**
- **Stripe从Hugging Face迁移至vLLM后，推理成本降低73%**

### 5.3 显存优化效果

传统推理方式需要为每个序列预分配最大长度的连续显存，造成大量浪费。vLLM的PagedAttention机制将KV缓存分为固定大小的页面（通常每页16个token），按需动态分配：

- **消除60-80%的显存碎片浪费**
- **相同显存可容纳更大batch，支持更多并发请求**


## 六、结果分析

### 6.1 vLLM速度提升的核心原因

**1. PagedAttention（分页注意力）**

传统KV Cache需要连续显存空间，随着序列增长会产生大量碎片。PagedAttention将KV Cache划分为固定大小的Block，像操作系统管理虚拟内存一样管理GPU显存：
- 非连续存储：KV Cache块可分散在GPU显存各处
- 动态分配：只在实际增长时分配内存
- 内存共享：相同prompt前缀可跨请求共享缓存

**2. 连续批处理（Continuous Batching）**

传统静态批处理需等所有请求完成后才返回。vLLM在每个token生成后动态调度：
- 已完成请求立即退出，新请求即时加入
- GPU始终满负荷工作，利用率大幅提升

**3. API Server与推理引擎分离**

vLLM v0.6.0将HTTP服务与推理引擎分离到不同进程，避免CPU网络处理阻塞GPU执行，显著降低延迟。

### 6.2 实验结论

1. **vLLM在吞吐量上显著优于Hugging Face Transformers**，尤其在并发场景下优势更为明显
2. **PagedAttention是性能提升的核心**，通过高效的显存管理消除了传统方案60-80%的浪费
3. **vLLM是生产级LLM推理服务的事实标准**，被Meta、Mistral AI、Cohere、IBM等广泛采用


## 七、作业提交清单

- [x] 实验报告（本文档）
- [x] 环境部署截图
- [x] vLLM服务启动日志截图
- [x] 性能测试结果截图（含HF Pipeline和vLLM对比）
- [x] 代码文件（benchmark_hf.py、benchmark_vllm.py）


## 八、参考命令汇总

```bash
# 1. 环境创建
conda create -n vllm_env python=3.10 -y
conda activate vllm_env
pip install vllm openai requests

# 2. 启动服务
vllm serve Qwen/Qwen2-0.5B-Instruct --host 0.0.0.0 --port 8000 --dtype float16

# 3. 健康检查
curl http://localhost:8000/health

# 4. 性能测试（vLLM内置）
python -m vllm.benchmarks.benchmark_serving \
    --backend openai \
    --model Qwen/Qwen2-0.5B-Instruct \
    --dataset-name random \
    --num-prompts 50 \
    --request-rate 10 \
    --max-tokens 128
```
